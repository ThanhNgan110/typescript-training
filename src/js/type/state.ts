interface State {
  id: string;
  countryId: number;
  name: string;
  // setState(states: State[]): void;
  // getState(): State[];
  // getStateByCountry(countryId: Number): State[];
}

export default State;
